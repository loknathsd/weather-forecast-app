import React from 'react';

const WeatherInfo = ({weather}) => {
    console.log(weather)
    return (
        <div>
          weather information detail for every country 
        </div>
    );
};

export default WeatherInfo;